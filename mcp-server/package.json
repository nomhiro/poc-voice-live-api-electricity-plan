{
  "name": "mcp-server-electricity-support",
  "version": "1.0.0",
  "description": "MCP Server for Electricity Customer Support",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@azure/cosmos": "^4.2.0",
    "@azure/functions": "^4.6.0",
    "nodemailer": "^6.9.16"
  },
  "devDependencies": {
    "@types/node": "^22.x",
    "@types/nodemailer": "^6.4.17",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
